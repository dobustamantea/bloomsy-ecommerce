export const STORE_CONFIG = {
  /**
   * Mostrar el banner "Estamos en construcción" en todo el sitio.
   * Cambiar a `false` para desactivarlo completamente (cero impacto).
   */
  isComingSoon: true,
};
